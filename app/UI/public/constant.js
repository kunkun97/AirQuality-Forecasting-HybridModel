// export const normalize_aqi = (a) => a * 1.5;
export const normalize_aqi = (a) => a;
// export const normalize_pollu = (a) => {
//   // Generate a random number between 1 and 4
//   const randomValue = Math.random();

//   // Determine the output based on the random value
//   if (randomValue < 0.5) {
//     return 1; // Higher chance for 1
//   } else if (randomValue < 0.75) {
//     return 2; // Chance for 2
//   } else if (randomValue < 0.9) {
//     return 3; // Chance for 3
//   } else {
//     return 4; // Higher chance for 4
//   }
// };
export const normalize_pollu = (a) => a;
